package com.aop.utils;

import com.aop.model.Car;

import java.util.ArrayList;
import java.util.List;

public class Observable {


    /*private List<Observer<String>> observers;

    public Observable() {
        observers = new ArrayList<>();
    }

    public void addObserver(Observer<String> observer) {
        observers.add(observer);
    }

    public void removeObserver(Observer<List<Car>> observer) {
        observers.remove(observer);
    }

    public void notifyObservers(String message) {
        observers.forEach(observer -> observer.update(message));
    }

    public List<Observer<String>> getObservers() {
        return observers;
    }

    public void setObservers(List<Observer<String>> observers) {
        this.observers = observers;
    }*/
}
